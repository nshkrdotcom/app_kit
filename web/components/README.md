# AppKit Web Components

Redaction-aware operator UI component contracts. The package accepts only
refs, hashes, bounded excerpts, timestamps, decision classes, and proof links.
It rejects raw prompt, memory, provider, eval, credential, token, secret, and
private-state fields before a product web shell can render them.
