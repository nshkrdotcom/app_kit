# AppKit Cost Dashboard

DTO-only cost and budget dashboard contracts. The package renders cost classes,
amount classes, meter refs, threshold policy refs, and budget decision refs
without raw provider account ids or native amount values.
