# AppKit Cost Surface

DTO-only cost breakdown, fact projection, and export request surface.

This package does not import the cost ledger. It validates and projects
ref-only cost data with bounded amount classes for operators.
