# AppKit Context Surface

DTO-only context packet, route, model invocation, eval, and operator review
surface for product-safe context execution readback.

The package exposes refs, hashes, bounded statuses, and operator state. It does
not expose raw prompts, memory bodies, provider payloads, credentials, lower
selectors, or lower owner structs.
