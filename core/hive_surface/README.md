# AppKit Hive Surface

DTO-only multi-agent coordination surface. It projects JidoHive coordinator,
message, shared-memory, and coordination-pattern records without exposing raw
message bodies, prompt bodies, provider payloads, memory bodies, tool outputs,
secrets, or skill private state.
