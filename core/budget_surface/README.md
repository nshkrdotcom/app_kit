# AppKit Budget Surface

DTO-only budget set, view, exhaustion, override, and audit projection surface.

This package validates bounded refs and classes only. It does not import the
budget enforcement engine directly.
