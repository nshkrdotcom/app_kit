# Surfaces

The initial surface categories are expected to be:

- chat-facing surfaces
- typed domain-facing surfaces
- operator-facing surfaces

This repository exists to make those northbound surfaces explicit and reusable.
